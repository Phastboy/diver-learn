   const const const...const const const...const const..    const handleVibrate = () => {
        if (window.navigator.vibrate) {
            window.navigator.vibrate(50);
        }
    c const.onst. };
 const.
